<main>
    <h1>The Future of the hero world</h1>

    {{#each students }} 
    <div class="student-container">
        <img src="{{ img }}"/>

		<h3><a href="{{ id }}">{{ name }}</a></h3>

    </div>

    {{/each}}
</main>